# 勉強ノート
event.rs以降rsの実装時に勉強した内容を示す。

# Event.rs編

## derive
構造体や列型に対してのふるまいを追加する。  
| Trait名      | 役割                                         |
| ------------ | -------------------------------------------- |
| `Debug`      | `println!("{:?}", x)` で中身を表示できる     |
| `Clone`      | `.clone()` でコピーできる                    |
| `PartialEq`  | `==` で比較できる                            |
| `Eq`         | 完全一致の比較（`PartialEq` とセットで使う） |
| `PartialOrd` | `<`, `>` などの大小比較ができる              |
| `Ord`        | ソート可能（`PartialOrd` とセットで使う）    |

```rust:event.rs
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum EventType {}
```
EventTypeに対していろいろな機能  

## sort()
ベクタ内の要素を並べる。
```rust:event.rs
let mut events = vec![e2, e3, e1];
events.sort();
```
vec!は動的配列。sort()で並び替え。    
sort()はOrdに従って並び替える。  
Ordはcmp()によって順番を定義。  
Ordは*トレイト*といい、トレイトは標準機能(sort等)の設定用関数みたいなもの。  
cmp()とい関数名はRustが指定。  
sort()は勝手にOrdを読み取るイメージ。
```rust:event.rs
// Event型のためのOrdのトレイトを実装
impl Ord for Event{
    fn cmp()
}
//Event型を定義しstruct上で動作する関数の定義
struct Event{}
impl Event{
    fn new()
}
```
| トレイト    | 必須メソッド | 説明                     |
| ----------- | ------------ | ------------------------ |
| `Ord`       | `cmp()`      | 完全な順序比較           |
| `PartialEq` | `eq()`       | 等価比較（`==`）         |
| `Clone`     | `clone()`    | 値の複製                 |
| `Hash`      | `hash()`     | ハッシュ値の生成         |
| `Display`   | `fmt()`      | ユーザー向け表示（`{}`） |
| `Debug`     | `fmt()`      | デバッグ表示（`{:?}`）   |

## PartialOrd, PartialEq, Eq
内容はderiveで定義したのでそっち参照。  
Ordのみだと定義できない場合(Naneを含む場合等)のために必要。  
Ordを使う場合はこれらをすべて示す必要がある。  
Ordを実装すればエラーで作る必要があるトレイトを教えてくれる。  
めんどい

## Ordering::EqualとPartialEQ::eq()の違い
Orderingの方は順序が同じ。  
PartialEq::eq()は等価性があるかを判定する責務
``` Rust:event.rs
Ordering::Less     // 左が小さい
Ordering::Equal    // 同じ
Ordering::Greater  // 左が大きい
```

## Selfとself
Selfはstructやenumそのもの  
selfはインスタンス(型の値)

## 参照と所有
&参照は値を参照するのみのためeqの比較は参照。  
所有は値を受け取り使う可能性がある。  
Ord関連は比較だけで十分のため&参照のみ。  

## Eventでderiveは定義できない
二重定義できない。  
deriveは自動定義、EventにはOrdを書いていない。  
もしderiveにOrdを入れると、f64ではNaneを許すため使えない。

# queue.rs編
## Vecとvec!
Vecは型名   *Vec<i32>*  
vec!はマクロ、ベクターを作る  
let v = vec![1,2,3]
## assert_eq!
implementでdebugが必要
## Noneの確認方法
matchとif letがある  
| 書き方   | 目的                           | 使いどころ               | 変数束縛                |
| -------- | ------------------------------ | ------------------------ | ----------------------- |
| `match`  | すべてのパターンを網羅的に処理 | 明示的な分岐が必要なとき | ✅ 可能                  |
| `if let` | 特定のパターンだけ処理         | 一部だけ扱いたいとき     | ✅ 可能（1パターン限定） |

| 判断軸                     | 適した書き方             |
| -------------------------- | ------------------------ |
| すべてのケースを処理したい | `match`                  |
| 一部のケースだけ処理したい | `if let`                 |
| 変数を束縛して使いたい     | 両方可能                 |
| 可読性を重視したい         | `if let`（限定用途なら） |

```Rust:match
match some_option {
    Some(val) => println!("Got value: {}", val),
    None => println!("No value"),
}
```
```Rust:if let
if let Some(val) = some_option {
    println!("Got value: {}", val);
}
// None の場合は何もしない
```

# scheduler.rs編
## BinaryHeap
Rust標準ライブラリ  
最大値優先のヒープ構造、pop()で最大値が取り出される  
ヒープ構造の箱を作る
### Heapとsortについて
sortはVecの順番を変える。この時にPartialOrdを参照している。(Ordが定義されていたらそちらが優先)  
BinaryHeapはpushしたタイミングで、Ordに定義される順番に保存される。  
今回は時間が小さい順で定義したかったから、event.rsのOrdのトレイトを逆順で定義した。  
| 操作                     | 参照されるトレイト         | デフォルトの動作             | 小さい順にするには？                     |
|--------------------------|----------------------------|------------------------------|------------------------------------------|
| `vec.sort()`             | `PartialOrd`（または `Ord`） | 昇順（小さい順）             | そのままでOK                             |
| `BinaryHeap::push()` / `pop()` | `Ord`                      | 降順（大きい順が優先）       | `Ord` を逆順に定義する必要あり           |

